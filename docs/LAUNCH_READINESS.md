# 正式发布核验记录

核验日期：2026-09-16（2026-09-17 更新测试计数）。本记录是发布准备证据，不代表已上传、已提审、已获准运营或已上线。

## 已确认

- 主开发目录：`/Users/worldpeace/Documents/ChatGPT/Wechat-Programs`。
- 微信项目目录：`/Users/worldpeace/WeChatProjects/reborn-career-new`。
- 微信项目 AppID：`wxd7051acac7211f6a`；配置为小游戏，非游客模式。仅这些配置不能证明账号主体、正式发布或变现资格。
- 仓库内 `project.config.json` 当前的 `appid` 是 `wxaa230e0fddc4f8c6`，与上面的截图 AppID 不一致，且没有 `touristappid` 字段。两个值都不是“已确认可发布的正式 AppID”，发布前必须由开发者本人在正式后台核对后统一。
- `npm run check` 成功，135 项测试通过；176 个事件、9 个结局。
- 两个目录的 `src/` 内容一致，微信项目入口为 `./src/ui/app`。
- 发布包排除未引用的模板目录 `archive/`、`audio/`、`images/`、`js/` 及 README、模板 lint 配置；文件未删除，AppID 未改。
- 当前平台层只使用设备窗口信息、触摸事件、本地存档和前后台生命周期；尚未接入正式广告或虚拟支付，也没有任何网络请求或登录接口。

## 必须在微信后台核验

用户提供的后台截图顶部明确显示“小游戏测试号”，截图 AppID 与当前微信项目一致。因此当前项目绑定的是测试号，不应作为正式发布或广告资格已具备的证据。下一步需要取得正式小游戏账号及其 AppID，并按正式后台要求核验资质；现有游戏代码无需重写。截图内的 AppSecret 不记录、不使用，已建议用户自行重置。

2026-09-17 补充实测证据（微信开发者工具 CLI）：用仓库目录直接 `cli preview` 时，工具取仓库 `project.config.json` 里的 `wxaa230e0fddc4f8c6`，服务端返回 `登录用户不是该小程序的开发者`（code 10）。**该 AppID 不属于当前登录账号，不能用于预览，更不能作为正式发布 AppID。** 改用本机微信项目绑定的 `wxd7051acac7211f6a`（后台截图对应的“小游戏测试号”）后预览成功，打包 538.3 KB；二维码仅供本机内部体验，不代表已提审或已上线。两个值都需由开发者在正式后台确认后再统一。

- 主体、管理员/开发者权限、账号是否为可正式发布的小游戏账号。
- 名称、类目、版权证明、自审材料、游戏相关审批/备案及 APP 备案的实际要求与状态。
- 隐私申报、客服投诉渠道、适龄/实名/防沉迷要求与接入状态。
- 发布截图、图标与介绍素材权利。
- 流量主是否可开通、真实广告位、结算资料与协议。

以上均未取得本轮后台页面证据，不作“缺证确定”或“已经完成”的推断。备案不替代其他依法所需审批，不能依据非官方“免版号”文章决定公开运营。

## 仍需产品验收

- 苹果、安卓真机的前后台切换、断网、存储不足、滚动和存档恢复。
- 请托、送礼、刑事判决等故事的人工语境复核；50% 获刑参数只属于虚构游戏模型，不得用作现实贪腐率宣传。
- 无广告首发与广告版本应分别验收；不得接假广告位或提前声称已赚钱。

## 本轮阻塞与下一步

浏览器控制的库存读取与微信公众平台打开连续超时，未能读取正式管理后台；微信开发者工具本身可读取。

请开发者本人在浏览器登录微信公众平台，提供“账号基本信息、备案状态、版本管理”页面的脱敏截图或恢复浏览器控制。不要提供身份证、银行卡、密码、短信验证码或 AppSecret。

取得后台证据后，完成材料与内容核验，上传真实候选版本；正式提审、承诺签署、发布及广告结算协议确认分别在对应动作前确认。不得隐藏功能、换错类目或审核后替换内容绕过审核。
